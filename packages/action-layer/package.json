{
  "name": "@venture/action-layer",
  "version": "0.1.0",
  "description": "The gate every irreversible agent action passes through: policy first, audit always.",
  "type": "module",
  "main": "./dist/src/index.js",
  "types": "./dist/src/index.d.ts",
  "exports": {
    ".": "./dist/src/index.js",
    "./schema.sql": "./schema.sql"
  },
  "files": [
    "dist",
    "schema.sql"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test dist/test/*.test.js",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "peerDependencies": {
    "pg": "^8"
  },
  "peerDependenciesMeta": {
    "pg": {
      "optional": true
    }
  },
  "devDependencies": {
    "@types/node": "^22",
    "typescript": "^5.6.0"
  },
  "license": "UNLICENSED"
}
